node_modules/
.next/
.env
.env.local
*.log
.DS_Store
sample-data/drops/
sample-data/audit.jsonl
sample-data/reservations.jsonl
web/public/media/
.superpowers/
web/data/catalog.json
web/data/sku-ledger.json
web/tsconfig.tsbuildinfo
sample-data/transfers/
ingest-inbox/
sample-data/reference/
pipeline/stitch/.venv/
pipeline/stitch/reports/
web/data/stitch-reports/

# Standalone stage viewer (viewer/) — deps and large model kept out of git
viewer/node_modules/
# The 48MB BMW model is treated like web/public/media: gitignored, rsynced to the
# server. Default car (Ferrari) is committed, so the viewer works without it.
viewer/public/models/bmw_m5.glb
web/public/stage/models/bmw_m5.glb
