node_modules/
.next/
.env
.env.local
*.log
.DS_Store
sample-data/drops/
sample-data/audit.jsonl
sample-data/reservations.jsonl
web/public/media/
.superpowers/
web/data/catalog.json
web/data/sku-ledger.json
web/tsconfig.tsbuildinfo
sample-data/transfers/
ingest-inbox/
sample-data/reference/
pipeline/stitch/.venv/
pipeline/stitch/reports/
web/data/stitch-reports/

# Stage viewer (viewer/) — only its install deps stay out of git; the models
# (incl. the 48MB bmw_m5.glb) are committed so the demo is self-contained.
viewer/node_modules/
